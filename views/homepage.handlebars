<!-- Loop over each object in the blog array -->
{{#if logged_in}}
{{#each blogs as |blog| }}
<div class="row mb-4 blog justify-center" blog-id={{blog.id}}>
  <div class="col-md-6 titleContainer">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      
      <a href="/blog/{{blog.id}}">{{blog.title}}</a>
    </h2>  
    <p>Posted by {{blog.user.name}} on {{format_date blog.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{blog.content}}
    </p>
  </div>
    {{!-- <div class="col-md-6 justify-center blog-block">
      <textarea rows="5" cols="20"></textarea> 
      <div class="col">
      <button class="comment-btn col">ADD COMMENT</button>
      </div>
    </div> --}}
</div>
{{/each}}
{{else}}
{{#each blogs as |blog| }}
<div class="row mb-4 blog justify-center" blog-id={{blog.id}}>
  <div class="col-md-6">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      
      <a href="/blog/{{blog.id}}">{{blog.title}}</a>
    </h2>  
    <p>Posted by {{blog.user.name}} on {{format_date blog.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{blog.content}}
    </p>
</div>
{{/each}}
{{/if}}